sudo: required
dist: trusty
language: node_js
node_js:
    - "6.3.1"
addons:
  packages:
    - unzip
    - google-chrome-stable
script:
    - npm run test-e2e -- --env ci
before_script:
    - npm install
before_install:
    - openssl aes-256-cbc -K $encrypted_1b5469661a88_key -iv $encrypted_1b5469661a88_iv -in deploy_key.enc -out deploy_key -d
