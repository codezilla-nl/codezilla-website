sudo: required
dist: trusty
language: node_js
node_js:
    - "6.3.1"
addons:
  packages:
    - unzip
    - google-chrome-stable
script:
    - npm run test-e2e -- --env ci
before_script:
    - npm install
